import 'package:todolist_flutter/entity/manager_deps.dart';

e(ManagerDeps deps) => deps.logger.e;
w(ManagerDeps deps) => deps.logger.w;
i(ManagerDeps deps) => deps.logger.i;
d(ManagerDeps deps) => deps.logger.d;